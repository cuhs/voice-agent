name: voice-agent-backend
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi>=0.100.0
      - uvicorn[standard]>=0.23.0
      - pydantic-settings>=2.0.0
